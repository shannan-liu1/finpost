"""Evaluation harness for FinPost post-training experiments.

This package provides the FinChain evaluation infrastructure for deterministic
exact-answer scoring. The registry (sources.py) decouples source-specific
parsing from the evaluation CLI.
"""

# Notice

FinPost's repo code, documentation, configs, notebooks, tests, and compact
result metadata are released under the MIT license in `LICENSE`.

This repository references external assets that are not relicensed here:

- Qwen model weights are hosted separately on Hugging Face and remain under
  their upstream model licenses and terms.
- FinChain template assets come from the upstream FinChain project and remain
  under that project's license and terms.
- Generated FinChain v2 JSONL rows under `data/finchain_v2_template_disjoint/`
  are derived from upstream FinChain template assets and are included for
  reproducibility.
- Local checkpoints, full eval dumps, caches, credentials, and intermediate
  source catalogs are intentionally not committed.

When reproducing a run, check the upstream license for each downloaded model,
dataset, or template repository before redistribution.
